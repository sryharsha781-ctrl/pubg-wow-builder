export const metadata = {
  title: "PUBG WOW AI Map Builder",
  description: "AI PUBG WOW Map Builder MVP",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
